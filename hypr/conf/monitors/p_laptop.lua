-- -----------------------------------------------------
-- Monitor Setup
-- name: "Private Laptop"
-- -----------------------------------------------------

-- Use the preferred resolution and auto position for the built-in display
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 })
