-- Hyprland 0.56+ parses this file as Lua; hyprlang was removed upstream.
--
-- The real config lives in hyprland.lua. This shim only matters for sessions
-- started with `-c ~/.config/hypr/hyprland.conf` (a session launched before
-- hyprland.lua existed); /usr/bin/starthyprland prefers hyprland.lua on login,
-- so new sessions never read this file.
--
-- The previous hyprlang version is in git history:
--   git show HEAD:hypr/hyprland.conf

require("hyprland")
